# context

- **路径**：`./src/context`
- **职责**：React Context Provider：主题（theme-provider）、主题定制（theme-customization-provider）、字体（font-provider）、文字方向（direction-provider）、布局（layout-provider）、搜索（search-provider）

## 直接依赖（内部）

- components/command-menu
- config
- lib
