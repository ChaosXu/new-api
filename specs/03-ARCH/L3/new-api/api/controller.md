# 控制器

- **职责**：HTTP API 的请求处理器，处理用户、渠道、令牌、日志、计费、充值、订阅、支付、任务、Passkey 等各业务接口的请求逻辑；含审计 action 模板渲染。
- **覆盖代码**：`controller/`（全部控制器文件）
- **关键契约**：各资源的 CRUD handler、审计 action 模板

## 依赖（内部逻辑模块）

- 业务逻辑（service 全部业务能力）
- 渠道适配框架（渠道测试等直接调用适配器）
- 数据访问
- 鉴权（鉴权中间件、权限授权）
- 配置
