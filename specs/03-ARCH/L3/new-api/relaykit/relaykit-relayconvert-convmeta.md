# relaykit/relayconvert/convmeta

- **路径**：`./relaykit/relayconvert/convmeta`
- **职责**：定义转换器与宿主之间的转换上下文契约（Meta 接口与每请求选项 Options），并提供根据请求 DTO 推断 RelayFormat 的能力

## 直接依赖（内部）

- relaykit/dto
- relaykit/types
