# 中继辅助

- **职责**：中继链路的辅助函数集合，提供计费/定价、流式扫描与结果收集、模型映射、请求合法性校验等被编排层与适配器共用的能力。
- **覆盖代码**：`relay/helper/`（valid_request.go 请求校验、计费/定价函数、流式扫描等）
- **关键契约**：`maxTokensLimit` 等校验常量、计费/定价辅助函数、流式结果收集器

## 依赖（内部逻辑模块）

- 中继上下文（操作 RelayInfo）
- 业务逻辑（调用计费结算、渠道选择）
- 配置（计费倍率、运营设置、计费模式）
- 通用工具、常量、日志
