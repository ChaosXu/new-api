# 上传文件
> `POST /v1/files`
- **鉴权**：TokenAuth
- **用途**：上传文件供后续使用。**当前未实现**，固定返回 501。

## 请求
预期 multipart/form-data（遵循 OpenAI 原生规范），含 `file`/`purpose`。当前不会被处理。

## 响应
HTTP 501 Not Implemented：
```json
{
  "error": {
    "message": "API not implemented",
    "type": "new_api_error",
    "param": "",
    "code": "api_not_implemented"
  }
}
```
