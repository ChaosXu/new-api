# 文件列表
> `GET /v1/files`
- **鉴权**：TokenAuth
- **用途**：列出已上传文件。**当前未实现**，固定返回 501。

## 请求
无请求体（预期遵循 OpenAI 原生规范，支持分页/用途过滤参数）。当前不会被处理。

## 响应
HTTP 501 Not Implemented：
```json
{
  "error": {
    "message": "API not implemented",
    "type": "new_api_error",
    "param": "",
    "code": "api_not_implemented"
  }
}
```
