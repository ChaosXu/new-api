# 分组列表
> `GET /api/group/`
- **鉴权**：AdminAuth（管理员）
- **用途**：获取全部用户分组名称（来自 GroupRatio 配置）。

## 请求
无

## 响应
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| data | string[] | 分组名称列表 |

示例：
```json
{ "success": true, "message": "", "data": ["default", "vip"] }
```

## 错误码
无
