# 获取关于内容

> `GET /api/about`

- **鉴权**：无（公开接口）
- **用途**：返回后台配置的「关于」页面内容（HTML / 纯文本）。

## 请求

无参数。

## 响应

```json
{
  "success": true,
  "message": "",
  "data": "<p>关于本站……</p>"
}
```

`data` 为字符串，可能为空字符串。

## 错误码

无。始终返回 HTTP 200。
