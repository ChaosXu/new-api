# 获取首页内容

> `GET /api/home_page_content`

- **鉴权**：无（公开接口）
- **用途**：返回自定义首页内容（HTML / 纯文本），用于首页渲染。

## 请求

无参数。

## 响应

```json
{
  "success": true,
  "message": "",
  "data": "<p>首页自定义内容……</p>"
}
```

`data` 为字符串，可能为空字符串。

## 错误码

无。始终返回 HTTP 200。
