# 获取用户协议

> `GET /api/user-agreement`

- **鉴权**：无（公开接口）
- **用途**：返回系统配置的用户协议内容（HTML / 纯文本）。

## 请求

无参数。

## 响应

```json
{
  "success": true,
  "message": "",
  "data": "<p>用户协议正文……</p>"
}
```

`data` 为字符串，可能为空字符串。

## 错误码

无。始终返回 HTTP 200。
