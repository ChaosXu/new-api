# 获取系统公告

> `GET /api/notice`

- **鉴权**：无（公开接口）
- **用途**：返回后台配置的系统公告 HTML / 纯文本内容，用于前端公告弹窗或横幅。

## 请求

无参数。

## 响应

标准信封 `{success, message, data}`。`data` 为字符串（公告原文，可能为空字符串）。

```json
{
  "success": true,
  "message": "",
  "data": "<p>欢迎使用 New API。</p>"
}
```

## 错误码

无。始终返回 HTTP 200。
