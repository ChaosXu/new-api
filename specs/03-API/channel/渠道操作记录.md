# 渠道操作记录

> `GET /api/channel/ops`

- **鉴权**：AdminAuth + RequirePermission(ChannelRead)
- **用途**：返回渠道测试相关的全局配置（如全局重试次数）。

## 请求

无。

## 响应

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| retry_times | int | 全局重试次数 |

```json
{
  "success": true,
  "data": { "retry_times": 3 }
}
```

## 错误码

无。
