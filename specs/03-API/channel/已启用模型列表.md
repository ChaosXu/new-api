# 已启用模型列表

> `GET /api/channel/models_enabled`

- **鉴权**：AdminAuth + RequirePermission(ChannelRead)
- **用途**：返回当前在任意已启用渠道中可用的模型列表。

## 请求

无。

## 响应

```json
{
  "success": true,
  "data": ["gpt-4o", "claude-3-5-sonnet"]
}
```

## 错误码

无。
