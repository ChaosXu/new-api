# 更新所有余额

> `GET /api/channel/update_balance`

- **鉴权**：AdminAuth + RequirePermission(ChannelOperate)
- **用途**：同步更新所有已启用、非多密钥渠道的余额；余额耗尽会自动禁用。

## 请求

无。

## 响应

```json
{
  "success": true,
  "message": ""
}
```

## 错误码

| 状态 | 说明 |
| --- | --- |
| 200(success=false) | 更新失败 |
