# 渠道模型列表

> `GET /api/channel/models`

- **鉴权**：AdminAuth + RequirePermission(ChannelRead)
- **用途**：返回系统内置支持的 OpenAI 模型列表。

## 请求

无。

## 响应

```json
{
  "success": true,
  "data": ["gpt-4o", "gpt-4o-mini"]
}
```

## 错误码

无。
