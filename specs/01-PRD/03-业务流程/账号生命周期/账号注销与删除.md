# 账号注销与删除

## 参与者

| 参与者 | 类型 | 职责 |
| --- | --- | --- |
| 普通用户 | 用户角色 | 主动注销自己的账号 |
| 管理员 | 用户角色 | 删除违规或废弃的用户账号（硬删除） |
| 网关接入层 | 系统 | 角色层级校验、数据清理、缓存与凭证作废 |
| 关系数据库 | 外部系统 | 软删除/硬删除用户及关联认证数据 |

## 流程图

```mermaid
flowchart TD
    START([Start - 触发账号删除]) --> G_WHO{谁来删除?}

    %% 用户自注销
    G_WHO -- 用户自注销 --> U1[POST /api/user/self/delete]
    U1 --> U2[网关：Root 用户禁止自删]
    U2 --> G_ROOT{当前用户是 Root?}
    G_ROOT -- 是 --> END_FAIL1([End - Cancel：超管不可自注销])
    G_ROOT -- 否 --> U3[事务内：IncrementUserAuthVersionWithTx 递增全局 auth version]
    U3 --> U4[GORM 软删除 user 行 保留数据但标记 deleted_at]
    U4 --> U5[发布 auth version tombstone 广播使 access token 失效]
    U5 --> U6[RevokeAllUserSessions user_deleted 作废所有会话]
    U6 --> U7[invalidateUserCache 清除用户缓存]
    U7 --> END_OK1([End - Success：账号已软注销，数据保留])

    %% 管理员硬删除
    G_WHO -- 管理员删除 --> A1[POST /api/user/manage action=delete]
    A1 --> A2[网关：canManageTargetRole myRole > targetRole 不可越级]
    A2 --> G_ROLE{可管理目标角色?}
    G_ROLE -- 否 --> END_FAIL2([End - Cancel：越权，拒绝])
    G_ROLE -- 是 --> A3[事务内 deleteUserAuthenticationData 一次性 Unscoped 清理]
    A3 --> A4[释放所有外部身份占位 releaseAllExternalIdentitiesWithTx]
    A4 --> A5[硬删 TwoFABackupCode / TwoFA / UserSession / AuthFlow / PasskeyCredential / Token]
    A5 --> A6[删除 user_oauth_bindings 自定义 OAuth 绑定]
    A6 --> A7[tx.Unscoped.Delete user 硬删 user 行]
    A7 --> A8[发布 auth version tombstone]
    A8 --> A9[invalidateTokensCache 清 Redis token 缓存]
    A9 --> A10[invalidateUserCache]
    A10 --> END_OK2([End - Success：账号及认证数据已彻底清除])
```

## 流程描述

账号删除分两类，数据清理粒度不同。

**用户自注销**（`controller/user.go:989` `DeleteSelf` → `model/user.go:853` `Delete`）是软删除：先校验 Root 用户不可自删（避免站点无主），事务内递增 `AuthVersion`（使所有 access token 失效），GORM 软删除用户行（保留数据），事务后广播 auth version、作废所有会话、清除缓存。软删后用户名/邮箱仍占用，防止重复注册与去重冲突。

**管理员硬删除**（`controller/user.go:957` `DeleteUser` → `model/user.go:877` `HardDelete`）需先通过角色层级网关 `canManageTargetRole`（`myRole > targetRole`，Root 除外）。通过后在事务内 `deleteUserAuthenticationData`（`user.go:914`）一次性 `Unscoped` 清理所有认证相关数据：释放外部身份占位、硬删 2FA/备用码/会话/AuthFlow/Passkey/Token/OAuth 绑定、最后硬删 user 行。事务后发布 tombstone 与清缓存。彻底删除后该用户名/邮箱可被重新注册。

**横切补偿**：任何改变用户安全态的操作（改密码、启禁 2FA、注册/删除 Passkey）都会触发 `IncrementUserAuthVersion` + `RevokeAllUserSessions`（或 `AdvanceCurrentSessionToUserVersion` 仅保留当前会话），这是贯穿账号生命周期各子流程的统一安全补偿动作。
