# 系统上下文

<!--
本模板由 code-to-context skill 使用。
填写规则：
- 系统作为黑盒，只描述"系统 ↔ 外部角色/外部系统"的边界与交互，不涉及系统内部结构。
- 外部角色（人）：按权限层级粗粒度归纳为抽象角色（如未认证访客/终端用户/管理员），以代码中实际存在的认证/授权分层为准，不细分到具体业务角色。
- 外部系统：运行时通过网络/IPC 交互的独立部署进程；不含编译进制品的第三方库（那些归 code-to-modules 的"导入模块"）。
- 系统职责：一句话浓缩系统对外提供的核心能力。
- Mermaid 图：系统居中，左连外部角色（圆角节点），右连外部系统（圆柱/方框），边标注交互方式。
- 命名用仓库/系统内的实际名称，不臆造。
- 删除所有 HTML 注释后再交付。
-->

## 系统职责

<!-- 一句话说明系统对外提供的核心能力。 -->

## 系统上下文图

<!--
Mermaid graph 或 flowchart：
- 系统节点居中（方框 []）。
- 外部角色用圆角节点 (())，置于左侧。
- 外部系统：存储类用圆柱 [(())]，服务类用方框 []，置于右侧。
- 边标注交互方式（如 HTTP API / 转发 / 读写 / 回调）。
-->

<!--
Mermaid graph 或 flowchart：
- 系统节点居中（方框 []）。
- 外部角色用圆角节点 (())，置于左侧。
- 外部系统：存储类用圆柱 [(())]，服务类用方框 []，置于右侧。
- 边标注交互方式（如 HTTP API / 转发 / 读写 / 回调）。
注：Mermaid 代码块内不要写 HTML 注释，会解析失败；占位文字直接写在节点/边里。
-->

```mermaid
graph LR
  User([外部角色])
  Sys[系统名]
  Ext[(外部系统)]

  User -->|交互方式| Sys
  Sys -->|交互方式| Ext
```

## 外部角色

<!--
按权限层级粗粒度归纳。以代码中实际存在的认证/授权分层为准，不细分到具体业务角色。
列：角色 | 说明（该角色的权限范围与典型操作）
-->

| 角色 | 说明 |
| --- | --- |
| <!-- 未认证访客 --> | <!-- 可访问的公开资源 --> |
| <!-- 认证用户 --> | <!-- 认证后可访问的资源与操作 --> |
| <!-- 管理员 --> | <!-- 后台管理/系统配置等高权限操作 --> |

## 外部系统

<!--
仅列"上游服务"——本系统为完成对外业务而主动调用的外部能力方（如上游 AI 提供商、支付网关、OAuth 提供商、邮件网关、人机验证 等）。
按职能分类，每类一张表。
列：系统 | 类别 | 与本系统的关系（本系统对其的调用方向、交互方式、用途）
不含：数据库/缓存/消息队列/对象存储等支撑系统运行的内部设施（归 L2/L3 部署视图）、编译进制品的第三方库。
-->

### <分类标题，如：上游 AI 服务>

| 系统 | 类别 | 与本系统的关系 |
| --- | --- | --- |
| <!-- 上游服务名 --> | <!-- 上游服务 --> | <!-- 本系统主动调用/被回调，用于什么 --> |

