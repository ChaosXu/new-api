# <!-- 本组件名 --> 的出站·外部关系

<!--
本模板由 code-to-components skill 使用，放置于 specs/03-ARCH/L2/{组件名}/relation/outbound-external.md。
分类判定：运行时，本组件主动调用外部系统（如转发上游 API、发起支付、发邮件）。外部角色（人）不归此类（人通常连入而非被调用）。
只列本组件的直接出站关系；经其他组件中转的不列。无此类关系则不建此文件。
命名用 L1/context.md 中的实际外部实体名。删除所有 HTML 注释后再交付。
-->

```mermaid
graph LR
  Me[<!-- 本组件 -->]
  Sys[(<!-- 外部系统 -->)]

  Me -- <!-- 方式 --> --> Sys
```

| 对方系统 | 方式 | 说明 |
| --- | --- | --- |
| <!-- 系统 --> | <!-- 转发/HTTP/SMTP 等 --> | <!-- 交互细节 --> |
