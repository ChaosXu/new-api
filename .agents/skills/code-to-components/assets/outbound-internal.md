# <!-- 本组件名 --> 的出站·内部关系

<!--
本模板由 code-to-components skill 使用，放置于 specs/03-ARCH/L2/{组件名}/relation/outbound-internal.md。
分类判定：运行时，本组件主动调用/连出其他组件（如托管其他组件的静态资源、调用其他组件接口）。
只列本组件的直接出站关系；经其他组件中转的不列。无此类关系则不建此文件。
命名用 components.md 中的实际组件名。删除所有 HTML 注释后再交付。
-->

```mermaid
graph LR
  Me[<!-- 本组件 -->]
  Other[<!-- 对方组件 -->]

  Me -- <!-- 方式 --> --> Other
```

| 对方组件 | 方式 | 说明 |
| --- | --- | --- |
| <!-- 对方 --> | <!-- 托管静态资源/HTTP 调用 等 --> | <!-- 交互细节 --> |
