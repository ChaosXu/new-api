# 组件关系

<!--
本模板由 code-to-components skill 使用。
填写规则：
- 描述组件之间的关系，回答"这些组件如何协作组成系统"。
- 组件关系图：用 Mermaid graph/flowchart，组件用方框 []，边标注关系类型与方向。
- 组件间交互表：逐对列出有关系的组件，含方向、时机（编译时 / 运行时 / 开发态）、方式、说明。
- 关系类型示例：编译时嵌入（go:embed / bundle）、运行时调用（HTTP / RPC）、父子进程（spawn）、托管静态资源、反向代理回源等。
- 命名用 components.md 中的实际组件名。
- 删除所有 HTML 注释后再交付。
-->

## 组件关系图

<!--
Mermaid graph 或 flowchart：
- 组件用方框 []。
- 边标注关系类型与方向（如"编译时嵌入""运行时调用""父子进程""托管静态资源"）。
- 若关系有时机区分（编译时 / 运行时 / 开发态），在边上注明。
-->

```mermaid
graph LR
  A[<!-- 组件A -->]
  B[<!-- 组件B -->]

  A -- <!-- 关系类型 --> --> B
```

## 组件间交互

<!--
逐对列出有关系的组件。仅列存在直接关系的组件对，不重复罗列无交互的组件。
列：组件 A | 组件 B | 时机 | 方式 | 说明
时机取值：编译时 / 运行时 / 开发态
-->

| 组件 A | 组件 B | 时机 | 方式 | 说明 |
| --- | --- | --- | --- | --- |
| <!-- 组件A --> | <!-- 组件B --> | <!-- 编译时/运行时/开发态 --> | <!-- 嵌入/HTTP/子进程/托管 等 --> | <!-- 关系细节 --> |
