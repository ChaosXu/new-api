# <!-- 本组件名 --> 的组件关系

<!--
本模板由 code-to-components skill 使用，每个组件一份，放置于 specs/03-ARCH/L2/{组件名}/relation.md。
填写规则：
- 从本组件的视角，描述它与其他组件之间的关系。标题中的组件名为本组件名。
- 只写本组件与他人的关系，不写别人之间的关系（后者归各自组件的 relation.md）。
- 本组件为中心关系图：用 Mermaid graph/flowchart，本组件为中心节点，周围是与本组件有直接关系的组件。
- 本组件的交互表：逐对列出本组件与其他组件的关系。
- 关系类型示例：编译时嵌入（go:embed / bundle）、运行时调用（HTTP / RPC）、父子进程（spawn）、托管静态资源、反向代理回源等。
- 命名用 components.md 中的实际组件名。
- 删除所有 HTML 注释后再交付。
-->

## 本组件为中心关系图

<!--
Mermaid graph 或 flowchart：
- 本组件作为中心节点（方框 []）。
- 周围是与本组件有直接关系的组件（方框 []）。
- 边标注关系类型与方向（如"编译时嵌入""运行时调用""父子进程""托管静态资源"）。
- 若关系有时机区分（编译时 / 运行时 / 开发态），在边上注明；开发态可用虚线 -.->
- 不画与本组件无直接关系的组件对。
-->

```mermaid
graph LR
  Me[<!-- 本组件 -->]
  Other[<!-- 对方组件 -->]

  Me -- <!-- 关系类型 --> --> Other
```

## 本组件的交互

<!--
逐对列出本组件与其他组件的关系。仅列与本组件有直接关系的对方组件。
列：对方组件 | 时机 | 方向 | 方式 | 说明
时机取值：编译时 / 运行时 / 开发态
方向取值：本组件→对方 / 对方→本组件
-->

| 对方组件 | 时机 | 方向 | 方式 | 说明 |
| --- | --- | --- | --- | --- |
| <!-- 对方组件 --> | <!-- 编译时/运行时/开发态 --> | <!-- 本组件→对方/对方→本组件 --> | <!-- 嵌入/HTTP/子进程/托管 等 --> | <!-- 关系细节 --> |
