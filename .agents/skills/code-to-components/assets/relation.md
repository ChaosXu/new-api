# <!-- 本组件名 --> 的组件关系

<!--
本模板由 code-to-components skill 使用，每个组件一份，放置于 specs/03-ARCH/L2/{组件名}/relation.md。
填写规则：
- 从本组件的视角，描述它的全部直接关系，分两部分：
  ① 与其他组件的关系（组件间）
  ② 与外部的关系（组件↔外部，L1 系统上下文在本组件上的落地）
- 只写本组件的直接关系：组件间不写别人之间的关系；外部只写本组件直接对接的（不经其他组件中转的）。
- 外部角色与外部系统的完整清单见 L1/context.md；本文件只标注其中由本组件直接承载的。
- 命名用 components.md 中的实际组件名、L1/context.md 中的实际外部实体名。
- 删除所有 HTML 注释后再交付。
-->

## 与其他组件的关系

<!--
组件间关系图（Mermaid graph/flowchart）：
- 本组件作为中心节点（方框 []）。
- 周围是与本组件有直接关系的组件（方框 []）。
- 边标注关系类型与方向（如"编译时嵌入""运行时调用""父子进程""托管静态资源"）。
- 若关系有时机区分（编译时 / 运行时 / 开发态），在边上注明；开发态可用虚线 -.->
- 不画与本组件无直接关系的组件对。
-->

```mermaid
graph LR
  Me[<!-- 本组件 -->]
  Other[<!-- 对方组件 -->]

  Me -- <!-- 关系类型 --> --> Other
```

<!--
组件间交互表：逐对列出本组件与其他组件的关系。仅列与本组件有直接关系的对方组件。
列：对方组件 | 时机 | 方向 | 方式 | 说明
时机取值：编译时 / 运行时 / 开发态
方向取值：本组件→对方 / 对方→本组件
-->

| 对方组件 | 时机 | 方向 | 方式 | 说明 |
| --- | --- | --- | --- | --- |
| <!-- 对方组件 --> | <!-- 编译时/运行时/开发态 --> | <!-- 本组件→对方/对方→本组件 --> | <!-- 嵌入/HTTP/子进程/托管 等 --> | <!-- 关系细节 --> |

## 与外部的关系

<!--
本组件与外部的关系图（Mermaid，可与上图合并为一张，也可分两张）：
- 本组件居中（方框 []）。
- 外部角色用圆角节点 (())。
- 外部系统用圆柱 [(())] 或方框 []。
- 边标注交互方式（如 HTTP API / 转发 / 读写 / 回调）。
- 只画本组件直接对接的外部实体；经其他组件中转的不画。
-->

```mermaid
graph LR
  Me[<!-- 本组件 -->]
  Role(<!-- 外部角色 -->)
  Up[(<!-- 上游服务 -->)]

  Role -- <!-- 交互方式 --> --> Me
  Me -- <!-- 交互方式 --> --> Up
```

### 外部角色

<!--
本组件直接对接的外部角色（人）。仅列与本组件直接交互的角色，经其他组件中转的不列。
列：对方角色 | 方向 | 方式 | 说明
-->

| 对方角色 | 方向 | 方式 | 说明 |
| --- | --- | --- | --- |
| <!-- 角色 --> | <!-- 对方→本组件/本组件→对方 --> | <!-- 浏览/调用 API/管理 等 --> | <!-- 交互细节 --> |

### 上游服务

<!--
本组件直接对接的上游服务（业务对话方，如上游 API、支付、OAuth、邮件网关）。仅列与本组件直接交互的，经其他组件中转的不列。
列：对方系统 | 方向 | 方式 | 说明
-->

| 对方系统 | 方向 | 方式 | 说明 |
| --- | --- | --- | --- |
| <!-- 系统 --> | <!-- 本组件→对方/对方→本组件/双向 --> | <!-- 转发/回调 等 --> | <!-- 交互细节 --> |

## 依赖的基础设施

<!--
承载本组件运行的支撑设施（数据库、缓存、消息队列、对象存储 等）。
与上游服务性质不同：上游服务是业务对话方，基础设施是"系统如何落地"的内部决策，故单列一节。
仅列本组件直接连接的设施，经其他组件中转的不列。
若本组件不直接连接任何基础设施，本节注明为空。
-->

```mermaid
graph LR
  Me[<!-- 本组件 -->]
  Fac[(<!-- 基础设施 -->)]

  Me -- <!-- 交互方式 --> --> Fac
```

| 基础设施 | 方向 | 方式 | 说明 |
| --- | --- | --- | --- |
| <!-- 设施 --> | <!-- 双向 --> | <!-- 读写/缓存/限流 等 --> | <!-- 交互细节 --> |
