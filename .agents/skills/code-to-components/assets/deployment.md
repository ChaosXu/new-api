# <!-- 本组件名 --> 的部署

<!--
本模板由 code-to-components skill 使用，每个组件一份，放置于 specs/03-ARCH/L2/{组件名}/deployment.md。
填写规则：
- 从本组件的视角，描述它在各部署形态中如何被打包、运行、与其他组件连接。
- 只描述涉及本组件的部署形态，聚焦本组件的打包/运行/连接方式。
- 若本组件在多种形态间有共性（如同一构建产物被复用、配置项可降级），在末尾"跨形态共性"节归纳；若无共性可省略。
- 命名用 components.md 中的实际组件名。
- 删除所有 HTML 注释后再交付。
-->

## 部署形态

<!--
每种涉及本组件的形态一个小节，标题为形态名（如"容器化单体""桌面应用""前端独立托管""裸机/systemd" 等）。
形态数量与划分由代码中的部署证据决定，不预设固定分类。
列：打包方式 | 运行方式 | 与其他组件的连接
-->

### <形态名>

| 项 | 说明 |
| --- | --- |
| 打包方式 | <!-- 本组件如何被打：多阶段镜像中的某阶段、electron extraResources、纯静态产物、裸二进制 等 --> |
| 运行方式 | <!-- 本组件如何跑：容器+compose、被父进程 spawn、systemd 服务、静态托管 等 --> |
| 与其他组件的连接 | <!-- 同源同端口 / 本地子进程 / 反代回源 / dev server 代理 等 --> |

## 跨形态共性

<!--
若本组件在多种形态间共享同一构建产物或可降级项，在此归纳。
若无共性可省略本节。
-->

- <!-- 共性描述 -->
