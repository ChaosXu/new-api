# 部署拓扑

<!--
本模板由 code-to-components skill 使用。
填写规则：
- 从部署描述文件（Dockerfile、docker-compose、打包配置、Makefile、systemd unit、CI 配置、README 部署章节、反向代理配置 等）归纳系统的部署形态。
- 每种形态说明：涉及组件、打包方式、运行方式、组件间连接（同源 / 子进程 / 反代回源 等）。
- 若存在跨形态共性（如同一二进制、数据层可降级、可选缓存 等），在末尾单列一节。
- 命名用 components.md 中的实际组件名。
- 删除所有 HTML 注释后再交付。
-->

## 部署形态

<!--
每种形态一个小节，标题为形态名（如"容器化单体""桌面应用""前端独立托管""裸机/systemd" 等）。
形态数量与划分由代码中的部署证据决定，不预设固定分类。
-->

### <形态名>

| 项 | 说明 |
| --- | --- |
| 涉及组件 | <!-- 参与该形态的组件名 --> |
| 打包方式 | <!-- 如何构建制品：多阶段镜像、electron-builder、纯静态产物、裸二进制 等 --> |
| 运行方式 | <!-- 如何运行：容器+compose、spawn 子进程、systemd 服务、静态托管 等 --> |
| 组件间连接 | <!-- 同源同端口 / 本地子进程 / 反代回源 / dev server 代理 等 --> |

## 跨形态共性

<!--
若多种形态共享同一运行核心或可降级项，在此归纳。例如：同一二进制、前端内嵌同源、数据层可降级、缓存可选、多节点需共享密钥 等。
若无共性可省略本节。
-->

- <!-- 共性描述 -->
