# <组件名> 模块索引

<!--
本模板由 code-to-modules skill 使用。
本文件是该组件所有模块的**可点击索引**（模块清单），每个模块的详情（职责+正向依赖）在独立文件里。
填写规则：
- 目标组件取自 specs/03-ARCH/L2/components.md 的"组件名"列。
- 内部模块：按职能分组，每行 = 模块 | 路径 | 职责（1 句）| 文件（链接到该模块详情文件）。
  - 非同构模块：链接到 <分组目录>/<模块名>.md（相对路径）。
  - 同构折叠模块：文件列留空（详情见同组下的折叠汇总文件，如 adaptors-sync.md）。
- 导入模块：第三方依赖按职能分类，每类一张表（同旧规则）。依赖不是"模块"，无独立文件，仍汇总在此。
- 命名用仓库内的实际名称（目录名 / package 名 / 依赖坐标），不臆造。
- 删除所有 HTML 注释后再交付。
-->

## 1. 内部模块

<!-- 按职能分组。每个分组一个三级标题（中文职能名 + 英文短目录名）。 -->

### <分组标题，如：中继核心与适配框架 (`relay-core`)>

| 模块 | 路径 | 职责 | 文件 |
| --- | --- | --- | --- |
| <!-- relay/common --> | <!-- ./relay/common --> | <!-- 一句话职责 --> | <!-- [relay-common.md](relay-core/relay-common.md) --> |
| <!-- （同构折叠组，如 39 个 channel 适配器） --> | <!-- ./relay/channel/* --> | <!-- 一句话共性职责 --> | <!-- 见 [adaptors-sync.md](adaptors-sync.md) --> |

### <分组标题，如：应用入口与路由装配 (`entry`)>

| 模块 | 路径 | 职责 | 文件 |
| --- | --- | --- | --- |
| <!-- main --> | <!-- ./ --> | <!-- 一句话职责 --> | <!-- [main.md](entry/main.md) --> |

## 2. 导入模块（第三方依赖）

<!-- 按职能分类，每类一张表。依赖命名用实际坐标（Go module path / npm 包名 / Maven 坐标 / PyPI 名）。 -->
<!-- 列：依赖 | 用途（据本组件源码中的实际 import/调用位置归纳，不照抄依赖 README） -->

### <分类标题，如：Web 框架与 HTTP 中间件>

| 依赖 | 用途 |
| --- | --- |
| <!-- github.com/gin-gonic/gin --> | <!-- Web 框架（路由、请求绑定、响应） --> |
