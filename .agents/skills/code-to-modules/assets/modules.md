# 模块归纳：<组件名>

<!--
本模板由 code-to-modules skill 使用。
填写规则：
- 目标组件取自 specs/03-ARCH/components.md 的"组件名"列。
- 内部模块：递归列出该组件源码树内的每一个叶子包/源码目录，不合并、不折叠。
- 导入模块：列出第三方依赖（剔除指向本仓库内部的 replace/workspace），按职能分类。
- 命名用仓库内的实际名称（目录名 / package 名 / 依赖坐标），不臆造。
- 删除所有 HTML 注释后再交付。
-->

> 针对 [`../components.md`](../components.md) 中的 **<组件名>** 组件（路径 `<组件路径>`，技术栈 `<技术栈>`），归纳其构成的全部模块。

## 一、内部模块

<!-- 按职能/层次分组。每个源码包/源码目录一行，列到叶子包粒度。 -->
<!-- 列：模块（package/目录名）| 路径 | 职责（1 句中文，据实际源码归纳） -->

### <分组标题，如：应用入口与路由装配>

| 模块 | 路径 | 职责 |
| --- | --- | --- |
| <!-- pkg/main --> | <!-- ./main --> | <!-- 一句话职责 --> |

### <分组标题，如：控制层与服务层>

| 模块 | 路径 | 职责 |
| --- | --- | --- |
| <!-- pkg/controller --> | <!-- ./controller --> | <!-- 一句话职责 --> |

<!-- 子 module（有独立 manifest 但无运行入口、只被宿主 import 的纯库）单独列出并注明 module 关系： -->

### 子 module（纯库型内部模块）

| 模块 | 路径 | 职责 | module 关系 |
| --- | --- | --- | --- |
| <!-- relaykit --> | <!-- ./relaykit --> | <!-- 一句话职责 --> | <!-- 独立 go.mod，根 module 以 require + replace 引入；无运行入口，属纯库 --> |

---

## 二、导入模块（第三方依赖）

<!-- 按职能分类，每类一张表。依赖命名用实际坐标（Go module path / npm 包名 / Maven 坐标 / PyPI 名）。 -->
<!-- 列：依赖 | 用途（据本组件源码中的实际 import/调用位置归纳，不照抄依赖 README） -->

### <分类标题，如：Web 框架与 HTTP 中间件>

| 依赖 | 用途 |
| --- | --- |
| <!-- github.com/gin-gonic/gin --> | <!-- Web 框架（路由、请求绑定、响应） --> |

### <分类标题，如：ORM 与数据库驱动>

| 依赖 | 用途 |
| --- | --- |
| <!-- gorm.io/gorm --> | <!-- GORM v2 ORM --> |
