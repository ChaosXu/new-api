# <逻辑模块名>

<!--
本模板由 code-to-modules skill 使用。一个逻辑模块一个文件，放在该模块所属功能域目录下。
填写规则：
- 模块名：该逻辑模块的职责名（如"渠道适配框架""计费结算"），不是目录名。
- 职责：1-2 句中文，说清该模块承担的核心逻辑能力。
- 覆盖代码：列出该模块覆盖的目录/文件（相对组件根，如 relay/channel/、relay/channel/*/）。
  一个模块可覆盖多个目录；多目录用顿号或列表分隔。这是"逻辑归类到代码"的可追溯桥梁。
- 关键契约：该模块对外暴露的核心 interface/struct/导出函数（代码内实际符号名）。
- 子能力：复杂模块才填，列内部 3-5 个子能力；简单模块删掉本节。
- 依赖：依赖的其他逻辑模块（按职责，非目录），据实际调用/引用提炼。
- 删除所有 HTML 注释后交付。
-->

- **职责**：<1-2 句，该模块承担的核心逻辑能力>
- **覆盖代码**：<列出覆盖的目录/文件，如 relay/channel/、relay/channel/*/、relay/relay_adaptor.go>
- **关键契约**：<对外暴露的核心 interface/struct/导出函数，如 Adaptor 接口、RelayInfo；无显著契约写"无">

## 内部子能力（若有）

<!-- 复杂模块才填。简单模块删除本节。 -->

- <子能力 1>：<一句话职责>
- <子能力 2>：<一句话职责>

## 依赖（内部逻辑模块）

<!-- 依赖的其他逻辑模块（按职责，非目录）。无依赖写"无"。 -->

- <逻辑模块 A>
- <逻辑模块 B>
