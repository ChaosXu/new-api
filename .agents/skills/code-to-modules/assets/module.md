# <模块名>

<!--
本模板由 code-to-modules skill 使用。一个模块一个文件，放在该模块所属职能分组目录下。
填写规则：
- 模块名用代码内实际 package/目录名（如 relay/common），不臆造。
- 路径用相对组件根的目录（如 ./relay/common）。
- 职责：1 句中文，据实际源码（包注释、doc.go、主要导出符号）归纳。
- 直接依赖：只列该模块直接 import 的本组件内其他模块，剔除标准库与第三方。无内部依赖写"无"。
- 删除所有 HTML 注释后交付。
-->

- **路径**：`<相对组件根的目录，如 ./relay/common>`
- **职责**：<1 句中文，据实际源码归纳>

## 直接依赖（内部）

<!-- 逗号分隔或列表。无内部依赖写"无"。 -->

- <模块 A>
- <模块 B>
