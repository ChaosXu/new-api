# <逻辑模块名>

<!--
本模板由 code-to-modules skill 使用。一个逻辑模块一个文件，放在该模块所属功能域目录下。
三节顺序固定：职责 → 契约（开放能力）→ 覆盖代码（+ 可选的子能力、依赖）。
填写规则：
- 模块名：该逻辑模块的职责名（如"渠道适配框架""计费结算"），不是目录名。
- 职责：1-2 句中文，说清该模块承担的核心逻辑能力。
- 契约（开放能力）：列该模块**对外开放的能力**，每条用能力短语 + 简短说明。
  契约是"能力"，不是代码符号（不列 interface/struct/函数签名）。
  例："统一的上游协议适配能力"、"按 API 类型分发到具体适配器的能力"。
  能力比代码符号更高层、更稳定（符号会重构，能力不变）。
- 覆盖代码：列出该模块覆盖的目录/文件（相对组件根）。一个模块可覆盖多个目录。
- 子能力：复杂模块才填，列内部 3-5 个子能力；简单模块删掉本节。
- 依赖：依赖的其他逻辑模块（按职责，非目录）。无依赖写"无"。
- 删除所有 HTML 注释后交付。
-->

## 职责

<1-2 句，该模块承担的核心逻辑能力>

## 契约（开放能力）

<!-- 列该模块对外提供的开放能力。每条 = 能力短语 + 简短说明。不列代码符号。 -->

- **<能力短语>**：<简短说明>
- **<能力短语>**：<简短说明>

## 覆盖代码

<列出覆盖的目录/文件，相对组件根，如 relay/channel/、relay/channel/*/、relay/relay_adaptor.go>

## 内部子能力（若有）

<!-- 复杂模块才填。简单模块删除本节。 -->

- <子能力 1>：<一句话职责>
- <子能力 2>：<一句话职责>

## 依赖（内部逻辑模块）

<!-- 依赖的其他逻辑模块（按职责，非目录）。无依赖写"无"。 -->

- <逻辑模块 A>
- <逻辑模块 B>
