# <组件名>

<!--
本模板由 code-to-modules skill 使用。
填写规则：
- 目标组件取自 specs/03-ARCH/L2/components.md 的"组件名"列。
- 本文件只写"模块间依赖关系"，模块清单与职责见同目录 modules.md。
- 依赖只列"内部模块彼此之间"的；第三方依赖见 modules.md 导入模块区，这里不重复。
- 只列直接依赖，不递归展开传递闭包。
- 命名用代码内实际名称（模块路径 / 符号名），不臆造。
- 删除所有 HTML 注释后再交付。
-->

## 1. 内部模块直接依赖

<!-- 每个有内部依赖的模块一行。只列该模块直接 import 的本组件内其他模块，剔除标准库/第三方。 -->
<!-- 列：模块 | 直接依赖（内部模块，逗号分隔） -->
<!-- 无内部依赖的叶子模块（如纯第三方封装）可不列入本表。 -->

| 模块 | 直接依赖（内部） |
| --- | --- |
| <!-- relay/channel/aws --> | <!-- relay/channel, relay/common, common --> |

## 2. 枢纽模块及其主要消费者

<!-- 只列枢纽模块：被 ≥5 个模块依赖，或承载核心契约（关键 interface/struct）。 -->
<!-- "主要消费者"可列代表性模块，数量多时注明"等 N 个"。 -->
<!-- 列：枢纽模块 | 主要消费者（内部） | 为何是枢纽（广泛依赖 / 核心契约） -->

| 枢纽模块 | 主要消费者（内部） | 为何是枢纽 |
| --- | --- | --- |
| <!-- relay/common --> | <!-- relay/channel/*（×39）、relay/helper、relay/constant ... --> | <!-- 广泛依赖：承载 RelayInfo 贯穿整个中继链路 --> |
| <!-- relay/channel --> | <!-- relay/channel/*（×39 适配器实现 Adaptor） --> | <!-- 核心契约：定义 Adaptor/TaskAdaptor 接口 --> |
